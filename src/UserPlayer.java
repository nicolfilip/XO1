public class UserPlayer extends Player implements Runnable {
    public UserPlayer(Game.Type playerType) {
        super(playerType);
    }
    public synchronized boolean checkTurn(){

    }

    @Override
    public void run() {
        while (true){
            try {
                Thread.sleep(500);
                if ()

            }catch ()
        }

    }
}
